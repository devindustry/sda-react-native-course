import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    counterText: {
        fontSize: 24,
        marginBottom: 20,
    },
    counterInput: {
        height: 40,
        borderColor: 'gray',
        borderWidth: 1,
        width: 100,
        textAlign: 'center',
    }

});